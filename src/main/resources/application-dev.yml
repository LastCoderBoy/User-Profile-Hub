spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: true
    locations: classpath:db/migration,classpath:db/dev

app:
  storage:
    local:
      root-dir: uploads
  server:
    base-url: ${SERVER_BASE_URL}